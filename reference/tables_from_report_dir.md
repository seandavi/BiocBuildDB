# Create tables for a report directory

This function creates a set of tables from a report directory.

## Usage

``` r
tables_from_report_dir(report_dir)
```

## Arguments

- report_dir:

  character(1) The directory containing the report files.

## Value

A list of data.frames with the tables from the report directory.
